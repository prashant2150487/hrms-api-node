# name: Deploy — Docker Build & Vercel

# on:
#   push:
#     branches: [main]

# jobs:
#   # ─────────────────────────────────────────
#   # JOB 1: Re-run lint + test before deploying
#   # ─────────────────────────────────────────
#   lint-and-test:
#     name: Lint & Test (pre-deploy gate)
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4

#       - name: Set up Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: 20.x
#           cache: "yarn"

#       - name: Install dependencies
#         run: yarn install --frozen-lockfile

#       - name: Run lint
#         run: yarn lint

#       - name: Run tests
#         run: yarn test

#   # ─────────────────────────────────────────
#   # JOB 2: Build & push Docker image
#   # ─────────────────────────────────────────
#   docker:
#     name: Build & Push Docker Image
#     runs-on: ubuntu-latest
#     needs: lint-and-test

#     steps:
#       - uses: actions/checkout@v4

#       - name: Log in to Docker Hub
#         uses: docker/login-action@v3
#         with:
#           username: ${{ secrets.DOCKER_USERNAME }}
#           password: ${{ secrets.DOCKER_PASSWORD }}

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       - name: Build and push
#         uses: docker/build-push-action@v5
#         with:
#           context: .
#           push: true
#           tags: |
#             ${{ secrets.DOCKER_USERNAME }}/${{ github.event.repository.name }}:latest
#             ${{ secrets.DOCKER_USERNAME }}/${{ github.event.repository.name }}:${{ github.sha }}
#           cache-from: type=gha
#           cache-to: type=gha,mode=max

#   # ─────────────────────────────────────────
#   # JOB 3: Deploy to Vercel
#   # ─────────────────────────────────────────
#   deploy-vercel:
#     name: Deploy to Vercel
#     runs-on: ubuntu-latest
#     needs: lint-and-test  # deploy even if docker job runs in parallel

#     steps:
#       - uses: actions/checkout@v4

#       - name: Install Vercel CLI
#         run: yarn global add vercel

#       - name: Pull Vercel environment info
#         run: vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}

#       - name: Build project via Vercel
#         run: vercel build --prod --token=${{ secrets.VERCEL_TOKEN }}

#       - name: Deploy to Vercel (production)
#         run: vercel deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }}