name: Deploy to AWS Lambda

on:
  push:
    branches:
      - main
      - develop
      - staging
  pull_request:
    branches:
      - main
      - develop

env:
  NODE_VERSION: '20.x'
  AWS_REGION: us-east-1

# jobs:
#   test:
#     name: Test & Lint
#     runs-on: ubuntu-latest
    
#     steps:
#       - uses: actions/checkout@v4
      
#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: ${{ env.NODE_VERSION }}
#           cache: 'npm'
      
#       - name: Install dependencies
#         run: npm ci
      
#       - name: Run linter
#         run: npm run lint
      
#       - name: Run tests
#         run: npm test

  # build-and-deploy:
  #   name: Build & Deploy to ${{ matrix.environment }}
  #   runs-on: ubuntu-latest
  #   needs: test
  #   if: github.event_name == 'push'
  #   strategy:
  #     matrix:
  #       include:
  #         - branch: develop
  #           environment: dev
  #           deployment_env: development
  #         - branch: staging
  #           environment: staging
  #           deployment_env: staging
  #         - branch: main
  #           environment: prod
  #           deployment_env: production
    
  #   environment:
  #     name: ${{ matrix.deployment_env }}
    
  #   steps:
  #     - uses: actions/checkout@v4
  #       if: github.ref == format('refs/heads/{0}', matrix.branch)
      
  #     - name: Setup Node.js
  #       if: github.ref == format('refs/heads/{0}', matrix.branch)
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: ${{ env.NODE_VERSION }}
  #         cache: 'npm'
      
  #     - name: Configure AWS credentials
  #       if: github.ref == format('refs/heads/{0}', matrix.branch)
  #       uses: aws-actions/configure-aws-credentials@v4
  #       with:
  #         aws-access-key-id: ${{ secrets[format('AWS_ACCESS_KEY_ID_{0}', matrix.environment)] }}
  #         aws-secret-access-key: ${{ secrets[format('AWS_SECRET_ACCESS_KEY_{0}', matrix.environment)] }}
  #         aws-region: ${{ env.AWS_REGION }}
      
  #     - name: Install dependencies
  #       if: github.ref == format('refs/heads/{0}', matrix.branch)
  #       run: npm ci
      
  #     - name: Build Lambda package
  #       if: github.ref == format('refs/heads/{0}', matrix.branch)
  #       run: |
  #         npm install --production
  #         npm run lambda:build
      
  #     - name: Deploy to Lambda (SAM)
  #       if: github.ref == format('refs/heads/{0}', matrix.branch)
  #       run: |
  #         sam deploy \
  #           --no-confirm-changeset \
  #           --stack-name hrms-api-${{ matrix.environment }} \
  #           --s3-prefix hrms-api-${{ matrix.environment }} \
  #           --capabilities CAPABILITY_IAM CAPABILITY_NAMED_IAM \
  #           --parameter-overrides \
  #             Environment=${{ matrix.environment }} \
  #             DBHost=${{ secrets[format('DB_HOST_{0}', matrix.environment)] }} \
  #             DBPort=${{ secrets[format('DB_PORT_{0}', matrix.environment)] || '5432' }} \
  #             DBName=${{ secrets[format('DB_NAME_{0}', matrix.environment)] }} \
  #             DBUser=${{ secrets[format('DB_USER_{0}', matrix.environment)] }} \
  #             DBPassword=${{ secrets[format('DB_PASSWORD_{0}', matrix.environment)] }} \
  #             RedisUrl=${{ secrets[format('REDIS_URL_{0}', matrix.environment)] }} \
  #             JwtSecret=${{ secrets[format('JWT_SECRET_{0}', matrix.environment)] }} \
  #             ClientUrl=${{ secrets[format('CLIENT_URL_{0}', matrix.environment)] }} \
  #             VpcSecurityGroupIds=${{ secrets[format('VPC_SECURITY_GROUP_IDS_{0}', matrix.environment)] }} \
  #             VpcSubnetIds=${{ secrets[format('VPC_SUBNET_IDS_{0}', matrix.environment)] }}
      
  #     - name: Get deployment outputs
  #       if: github.ref == format('refs/heads/{0}', matrix.branch)
  #       run: |
  #         aws cloudformation describe-stacks \
  #           --stack-name hrms-api-${{ matrix.environment }} \
  #           --query 'Stacks[0].Outputs' \
  #           --output table
      
  #     - name: Run post-deployment tests
  #       if: github.ref == format('refs/heads/{0}', matrix.branch)
  #       run: |
  #         API_ENDPOINT=$(aws cloudformation describe-stacks \
  #           --stack-name hrms-api-${{ matrix.environment }} \
  #           --query 'Stacks[0].Outputs[?OutputKey==`ApiEndpoint`].OutputValue' \
  #           --output text)
          
  #         echo "Testing health endpoint: $API_ENDPOINT/health"
  #         curl -X GET "$API_ENDPOINT/health" -H "Content-Type: application/json"

  # create-release:
  #   name: Create Release
  #   runs-on: ubuntu-latest
  #   needs: build-and-deploy
  #   if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    
  #   steps:
  #     - uses: actions/checkout@v4
      
  #     - name: Create GitHub Release
  #       uses: actions/create-release@v1
  #       env:
  #         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  #       with:
  #         tag_name: v${{ github.run_number }}
  #         release_name: Release v${{ github.run_number }}
  #         body: |
  #           Automated release of HRMS API
            
  #           Deployed to production on ${{ github.event.head_commit.timestamp }}
  #           Commit: ${{ github.event.head_commit.id }}
  #         draft: false
  #         prerelease: false
